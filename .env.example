# Frontend
VITE_API_URL=
VITE_WHATSAPP_NUMBER=923429619908

# Required for Vercel serverless functions (api/*.js)
# Must be configured in Vercel project dashboard:
# https://vercel.com/<project>/settings/environment-variables
# MONGODB_URI=
# JWT_SECRET=
# JWT_REFRESH_SECRET=
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=

# To prevent cold starts, set up a free cron job at https://cron-job.org
# that pings https://sadaat-electrical-store.vercel.app/api/health every 4 minutes.
# This keeps your serverless functions warm (Hobby plan idle timeout is ~5 min).
